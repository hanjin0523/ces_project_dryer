미해결
- 접속건조기 웹소켓으로 로직 변경예정
- 메뉴가 건조기가 변경될시에 선택된 메뉴는 그대로고 디테일레시피(스테이지,건조시간등)은 0번으로 변경되어 불러지는 현상처리
- on_off_timer함수에서 소켓온오프는 직접 컨트롤이 아니라 소켓함수 이용하는걸로 변경해야됨..

해결완료
23.08.30 
- 스레드로 N개의 메인보드 제어로직 완성 테스트 결과 오차발견
23.08.31 
- 스레드 종료시점에 맞춰 프론트도 동작멈추는 페이지로 로직변경완료
- 스레드 정지구현(플래그(is_running) 함수 이용을 통해 False, True 변경함수 별도 생성으로 스레드제어완)
- 메인보드전원on/off시에 소켓리스트에 자동으로 추가되고 해제되는 별도 제어로직 완료 
따로 보드에서 일정하게 쏴주는 정보가 없어서 senser1를 전송하면 바이트형태의 프레임이 들어오는데 그 프레임이 
없을 경우 해당 보드는 소켓리스트에서 del 시키는 방법으로 구현
(예외처리)연결된 소켓리스트 즉 보드가 모두 연결이 안될시에는 No client로 접속 없음을 알리며 사용자에서 얼럿창
23.09.02 - 23.09.05
- 스레드 정지시에 정지된 조건 값을 보전하고 그 값을 그대로 다시 사용하는 로직 구현
클래스 변수를 선언하여 객체화된 클래스 변수에 현재정지시점의 남은 값을 변수에 넣고 다시 on함수 작동시에 
정지시점의 값이 0이라면 기존과 같은로직 0이 아니라면 정지시점의값을 사용하여 타이머on을 구현함.
- 정지된시점에서 재가동시 마찬가지로 사용자에게 보여지는 뷰도 동일한 값을 보존 정지하여 그대로 다시 이어서 
동작하는 것처럼 구현완료됨
(예외처리)쓰레드가 여러번 형성될것을 방지해서 플래스변수로 제어완료함.
23.09.06 - 
- 스레드로 동작구현은 완료하였고, 예외처리도 잘되었으니 현재 프로젝트에서는 온도 습도 값을 통한 지능형으로 제어가
되어야함 그래서 큐를 활용하여 작업을 쌓아서 for반복문을 통해 하나하나 꺼내서 순차처리할계획.

